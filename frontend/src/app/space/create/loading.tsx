import { MainContainer } from '@/components/layout/SiteContainer'

export default function CreateSpaceLoading() {
  return <MainContainer className="py-8 text-sm text-slate-400">Memuat formulir pembuatan space...</MainContainer>
}
