# Components

Folder komponen reusable UI VoteChain.

Prioritas implementasi bertahap:
1. `ui/` (Button, Card, Badge, StepIndicator, HashBox)
2. `layout/` (GlobalNav, TabNav)
3. `voting/`, `admin/`, `space/`
